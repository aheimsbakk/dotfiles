---
when: 2026-03-23T18:27:07Z
why: update.sh VERSION constant was out of sync with .version and never updated on bumps
what: Sync update.sh VERSION to 0.3.1 and extend bump-version.sh to keep it in sync
model: opencode/claude-sonnet-4-6
tags: [fix, versioning, scripts]
---

Set `readonly VERSION` in `update.sh` to `0.3.1` (was `2.0.0`). Extended `scripts/bump-version.sh` to also update the `VERSION` constant in `update.sh` via `sed` on every bump, ensuring the two sources never drift again. Version remains 0.3.1.
